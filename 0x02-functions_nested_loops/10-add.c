#include "main.h"

/**
 * add - returns int type and takes two int type arguments
 *
 * @num1, @num2: The integers to be added
 *
 * Description: Adds integers an returns the result
 *
 * Return: Return two added int types
 */
int add(int num1, int num2)
{
return (num1 + num2);
}
