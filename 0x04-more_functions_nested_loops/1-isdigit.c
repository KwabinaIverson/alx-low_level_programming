#include "main.h"

/**
 * _isdigit - return an int type if c is a digit
 *
 * @c: The digit to check
 *
 * Return: 1 if its digit else 0
 */
int _isdigit(int c)
{
while (c >= '0' && c <= '9')
{
return (1);
}
else
{
return (0);
}
}
